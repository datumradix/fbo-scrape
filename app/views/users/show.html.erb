<p>
  Username:
  <%= link_to @user.username, edit_user_path(:current) %>
</p>

<p>
  Team Lead:
  <%= link_to "Scott Lamb", static_pages_criteria_path %>
</p>
<hr>

<h3>Status</h3>
<p>Seven day history based on your team's <%= link_to "search criteria", static_pages_criteria_path %> and evaluation activity.</p>
<div class="row">
  <div class="span2 offset0">
    <table class="table table-bordered">
      <tr><td>Opportunities</td><td><%= link_to @not_evaluated_count, opportunities_path(:set_filter => "Not Evaluated") %></td></tr>
      <tr><td>Watchlist</td><td><%= link_to @watchlist_count, opportunities_path(:set_filter => "Watchlist") %></td></tr>
      <tr><td>Reject</td><td><%= link_to @reject_count, opportunities_path(:set_filter => "Reject") %></td></tr>
    </table>
  </div>
</div>
<hr>

<h3>Comments</h3>
<%= will_paginate @comments %>
<table class="table table-hover">
  <thead>
    <tr>
      <th>Post Date</th>
      <th>Opportunity</th>
      <th>Evaluation</th>
      <th>Name</th>
      <th>Comment</th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td> <%= comment.opportunity.post_date.strftime("%b %d") %> </td>
        <td> <%= link_to comment.opportunity.opportunity, opportunity_path(comment.opportunity) %></td>
        <td> <%= comment.opportunity.management_evaluation %> </td>
        <td><%= comment.name %></td>
        <td><%= comment.comment %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @comments %>





