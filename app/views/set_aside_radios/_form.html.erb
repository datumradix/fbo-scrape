<%= form_for(@set_aside_radio) do |f| %>
  <% if @set_aside_radio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@set_aside_radio.errors.count, "error") %> prohibited this set_aside_radio from being saved:</h2>

      <ul>
      <% @set_aside_radio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
