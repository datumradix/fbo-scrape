<%= form_for(@solicitation_number) do |f| %>
  <% if @solicitation_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solicitation_number.errors.count, "error") %> prohibited this solicitation_number from being saved:</h2>

      <ul>
      <% @solicitation_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
