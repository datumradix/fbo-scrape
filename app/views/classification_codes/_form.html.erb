<%= form_for(@classification_code) do |f| %>
  <% if @classification_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@classification_code.errors.count, "error") %> prohibited this classification_code from being saved:</h2>

      <ul>
      <% @classification_code.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
